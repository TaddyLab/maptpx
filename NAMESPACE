useDynLib(maptpx)
import(slam)

export(stm_tfidf,
       normalize,
       rdir,
       topics,
       predict.topics,
       topicVar,
       expit,
       logit)

S3method(plot,topics)
S3method(predict,topics)
S3method(summary,topics)
